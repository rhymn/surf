# Local development (uses default postgres database)
NODE_ENV=development
PORT=5000

# Production (Render will set DATABASE_URL automatically)
# NODE_ENV=production
# DATABASE_URL=postgresql://user:pass@host:5432/dbname